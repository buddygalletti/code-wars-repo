function addKStrings(arr, k) {
    const n = arr.length;
    const newArr = [];
    if (k <= 0 || n === 0) {
        return ""
    }
    // const thing = Array.apply(null, arr).map(function (_, i) {return i;});
    for (let i = 0; i < n - k; i++) {
        
    }
    let maxLength = 0;

}


const test = ["zone", "abigail", "theta", "form", "libe", "zas", "theta", "abigail"];
console.log(addKStrings(test));